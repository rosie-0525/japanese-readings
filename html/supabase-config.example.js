// Copy this file to `supabase-config.js` (which is git-ignored) and fill in your
// own Supabase project's values. Find them in:
//   Supabase dashboard → Project Settings → API
//
// The anon (public) key is SAFE to expose in client code — Row-Level Security in
// db/schema.sql is what keeps each user's data private. Do NOT paste the service_role key here.
window.SUPA_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPA_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
